Use case: infographic-diagram
Asset type: TaskBeacon task flow diagram
Primary request: Create a clean, publication-ready task flow diagram as a timeline collection for the behavioral task described below.

Task: Visual Change Detection Task
Construct: visual working-memory capacity
Rows/conditions:
- SAME: the test array repeats every color at exactly the same positions.
- ONE COLOR CHANGE: the test array uses the same positions and changes exactly one square to a different color.

Timeline phases:
- SAME: Fixation (500 ms; no response; central white + on a near-black screen) -> Memory (100 ms; no response; four small uniquely colored squares around fixation) -> Retention (900 ms; no response; central white + only) -> Test (2000 ms max; F = SAME, J = CHANGE; same four colored squares at the same positions)
- ONE COLOR CHANGE: Fixation (500 ms; no response; central white + on a near-black screen) -> Memory (100 ms; no response; four small uniquely colored squares around fixation) -> Retention (900 ms; no response; central white + only) -> Test (2000 ms max; F = SAME, J = CHANGE; same positions with exactly one square shown in a new color)

Visual requirements:
- White background, landscape orientation, crisp dark text, restrained condition accent colors.
- One horizontal row per representative trial type.
- Each row contains exactly four participant-screen snapshots connected by a subtle arrow.
- Each screen snapshot shows the visible stimulus, not internal variable names.
- Use gray participant-screen boxes, thin black arrows, consistent row spacing, and subtle row separators.
- Place timing labels under each screen in compact text.
- Place condition labels at the left of each row.
- Use short labels only; avoid paragraphs inside the image.
- Make all text legible at normal document preview size.
- Leave a clean blank header band across the top 15-18% of the image. This band is reserved for a fixed title, Construct subtitle, and TaskBeacon logo lockup that will be added after generation.
- The test positions must match the memory positions exactly. In the change row, change the fill color of one square only; do not move, add, or remove squares.

Accuracy constraints:
- Do not invent phases, stimuli, condition names, keys, rewards, or timings.
- Do not add feedback after Test.
- Do not add people, lab equipment, decorative scenes, logos, or unrelated icons.
- Do not draw the task title, construct subtitle, any logo, watermark, brand mark, or TaskBeacon text inside the generated image.
- Draw only the timeline content below the blank header band.
- Preserve these exact terms: SAME, ONE COLOR CHANGE, Fixation, Memory, Retention, Test, 500 ms, 100 ms, 900 ms, 2000 ms max, F = SAME, J = CHANGE.

Style:
TaskBeacon scientific infographic style: clean vector-like raster image, organized spacing, gray screen boxes, restrained color accents, and a blank header-safe area.
