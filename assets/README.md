# Assets

This task uses PsychoPy drawing primitives only. The colored memory and test squares are generated from the palette and positions logged with every trial; no external media assets are required.
